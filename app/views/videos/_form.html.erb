<%= form_with(model: video) do |form| %>
  <% if video.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(video.errors.count, "error") %> prohibited this video from being saved:</h2>

      <ul>
        <% video.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.file_field :file, multiple: false, direct_upload: true, class: "file:border file:border-solid" %>
  </div>

  <p>
          <%= video.whisper_txt %>
  </p>

  <p>
          <%= video.whisper_tsv %>
  </p>
  <div>
    <%= form.submit class: "bg-blue-600 hover:bg-blue-700 py-2 px-4 border border-indigo-500 rounded shadow" %>
  </div>
<% end %>
